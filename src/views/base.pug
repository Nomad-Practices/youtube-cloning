doctype html
html(lang="ko")
    head
        // #{...}은 JS의 template literal 방식처럼 사용된다.
        title #{pageTitle} | Wetube
        // tag attribute들은 (...) 안에 작성한다.
        link(rel="stylesheet" href="https://unpkg.com/mvp.css@1.11/mvp.css")
    body
        header
            if fakeUser.loggedIn
                small Hello #{fakeUser.username}
            nav
                ul
                    if fakeUser.loggedIn
                        li 
                            a(href="/logout") Log Out
                    else
                        li 
                            a(href="/login") Login
            // "=변수명"은 변수의 값을 html tag 내의 text로 그대로 대입한다고 생각하면 된다.
            // template binding 개념과 비슷한듯?
            h1=pageTitle
        main
            block content  
    include partials/footer.pug