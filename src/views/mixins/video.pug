mixin video(video)
    div
        h4
            a(href=`/videos/${video.id}`)=video.title
        p=video.description
        ul
            each h in video.hashtags
                li=h
        small=video.createdAt
        hr